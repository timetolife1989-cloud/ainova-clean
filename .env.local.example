# =====================================================================
# AINOVA - Environment Variables Template
# =====================================================================
# Copy this file to .env.local and fill in your actual values
# .env.local is already in .gitignore - never commit it!
# =====================================================================

# =====================================================================
# Weather API Configuration (OpenWeatherMap)
# =====================================================================
# Sign up for a free API key at: https://home.openweathermap.org/users/sign_up
# Free tier: 1,000 calls/day, 60 calls/minute
# Expected usage: ~144 calls/day (10-minute refresh interval)
#
# Required: WEATHER_API_KEY - Your OpenWeatherMap API key
WEATHER_API_KEY=your_api_key_here
#
# Optional: WEATHER_LOCATION - City and country code (default: Budapest,HU)
# Format: "City,CountryCode" (e.g., "London,UK", "New York,US")
WEATHER_LOCATION=Budapest,HU
#
# Optional: WEATHER_UNITS - Temperature units (default: metric)
# Options: metric (Celsius), imperial (Fahrenheit), standard (Kelvin)
WEATHER_UNITS=metric
#
# Optional: WEATHER_LANGUAGE - Weather description language (default: hu)
# Options: hu (Hungarian), en (English), de (German), etc.
# Full list: https://openweathermap.org/current#multi
WEATHER_LANGUAGE=hu
# =====================================================================

# =====================================================================
# Setup Instructions
# =====================================================================
# 1. Sign up for OpenWeatherMap API:
#    https://home.openweathermap.org/users/sign_up
#
# 2. Get your API key from the dashboard:
#    https://home.openweathermap.org/api_keys
#
# 3. Copy this file to .env.local:
#    cp .env.local.example .env.local
#
# 4. Replace "your_api_key_here" with your actual API key
#
# 5. (Optional) Customize location, units, and language
#
# 6. Restart the development server:
#    npm run dev
# =====================================================================
